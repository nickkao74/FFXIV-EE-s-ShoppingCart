{
  "name": "ffxiv-ee-shopping-cart",
  "version": "2.0.0",
  "private": true,
  "description": "EE的FFXIV購物車 — Cloudflare Workers 版本",
  "scripts": {
    "dev": "wrangler dev",
    "db:init": "wrangler d1 execute ffxiv-ee-shopping-cart-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute ffxiv-ee-shopping-cart-db --remote --file=./schema.sql",
    "db:reset": "wrangler d1 execute ffxiv-ee-shopping-cart-db --local --command=\"DROP TABLE IF EXISTS orders; DROP TABLE IF EXISTS stock; DROP TABLE IF EXISTS events;\" && npm run db:init",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260804.1",
    "@types/node": "^26.1.2",
    "typescript": "latest",
    "wrangler": "latest"
  }
}
